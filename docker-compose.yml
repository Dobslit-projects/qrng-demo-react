services:
  web:
    build: .
    network_mode: "host"
    restart: unless-stopped
    environment:
      - PORT=3001
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3001"]
      interval: 30s
      timeout: 5s
      retries: 3
